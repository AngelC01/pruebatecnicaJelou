{
  "name": "products-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --exec node src/index.js",
    "start": "node src/index.js",
    "build": "echo 'no build step (plain JS)'",
    "migrate": "echo 'run migrations if any'",
    "seed": "echo 'run seeds if any'",
    "test": "echo 'no tests configured'"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mysql2": "^3.2.0",
    "zod": "^3.23.2",
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
